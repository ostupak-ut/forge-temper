import type { EdgeTypes } from '@xyflow/react'
import { FeedbackEdge } from './FeedbackEdge'

/** Module-level (stable) edge type map. */
export const edgeTypes: EdgeTypes = {
  feedback: FeedbackEdge,
}
